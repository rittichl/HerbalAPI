// import { body } from 'express-validator';

// export const createUserGroupValidation = [
//     body('group_code')
//         .notEmpty().withMessage('Group code is required')
//         .isString().withMessage('Group code must be a string')
//         .isLength({ max: 10 }).withMessage('Group code cannot exceed 10 characters'),

//     body('group_name')
//         .notEmpty().withMessage('Group name is required')
//         .isString().withMessage('Group name must be a string')
//         .isLength({ max: 100 }).withMessage('Group name cannot exceed 100 characters')
// ];

